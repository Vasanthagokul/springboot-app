apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: harness.virtusa.dev
    http:
      paths:
      - path: /*
        backend:
          service:
            name: harness11
            port:
              number: 80
